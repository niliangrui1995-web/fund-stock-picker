# 项目审计与调试

审计日期：2026-09-05。项目：`D:\vcp_hunter\基金持仓`，分支：`master`。

本次审查覆盖前端研究与保存、两类图表、反馈 Worker、数据采集与索引生成、发布回滚、依赖锁文件、自动化测试和本地生产构建。通过临时目录、模拟输入和真实浏览器复现问题后修复；未重采集或重建正式持仓数据，未提交、推送或部署。

## 已修复问题

| 问题与触发条件 | 修复后的行为 | 回归证据 |
| --- | --- | --- |
| 两个标签页同时打开后依次保存，后保存的旧页面覆盖其他页面的新组合 | 写前核对当前存储，冲突时停止覆盖、保留草稿、刷新列表；用户检查后可重试或另存 | Hook 双实例测试；真实两页依次保存 A/B、冲突后重试，最终两组合均保留 |
| Ctrl/Meta/Shift/Alt 或非左键点击研究页导航被离开保护拦截 | 仅普通左键触发当前页离开保护；修饰键沿用浏览器行为 | 6 个导航案例；真实 Ctrl+点击打开新标签，原页保留 |
| 持仓或报告解析失败仍以成功流程覆盖完整旧文件 | `parse_error`、`pdf_parse_error` 阻止发布；股票索引入口同样拒绝解析失败摘要；正常未披露状态不变 | 临时目录模拟失败，核对旧文件字节不变；验证正常 `no_data`、`out_of_period` |
| 非 Q2 构建被遗留 H1 文件阻断 | 仅 Q2 加载 H1 补充数据，Q2 仍严格验证年度、截止日和完整性 | Q1/Q3/Q4 跳过 H1；Q2 错期仍拒绝 |
| 多文件发布失败且 manifest 回滚被文件锁阻止后，清理删掉仍被引用的分片 | 失败清理先核对活动 manifest；引用新版本或不能读取时保留分片；保留既存版本、回滚备份及原始错误；回滚使用短锁重试 | 故障注入后核对活动分片 SHA、旧 manifest 备份及短锁恢复 |
| 反馈 POST JSON 为 `null` 时出现未捕获异常 | 非对象、数组和原始 JSON 值返回 400 | Worker 本地 VM 测试，禁止真实网络和邮件发送 |
| 1200 字中文留言加合法验证 token 超过旧 4096 字节预算 | 请求总预算改为 16 KiB，业务字段长度限制保持；超大头部和流式正文仍返回 413 | 中文最大长度通过前置校验；两类超限请求仍被拒绝 |
| 两融取消全部指数后右侧指数轴残留 | 更新时同时替换曲线和坐标轴，重新选中后恢复，保留缩放 | 真实 ECharts SVG 生命周期测试 |
| 集中度勾选曲线将已选缩放重置为全部 | 单独更新图例选择；数据区间变化仍重置缩放 | 真实 React 组件和 ECharts：取消/重选保留 `[40,70]`；切区间重置为 `[0,100]` |
| 直接运行 Vitest 误收集 Node 测试并报 6 个空测试套件 | Vitest 仅收集 `src`；新增 `npm test` 串联 Vitest、Node 和 Python 测试；Worker 测试接入发布门禁 | 默认 Vitest 和完整 `npm test` 均通过 |
| 锁文件含 Browserslist 4.28.2，与本机安装版本不一致 | 定向更新至 4.28.9 及其浏览器数据库依赖；按锁文件重新安装验证 | `npm ci --ignore-scripts --registry=https://registry.npmjs.org`；`npm audit` 为 0 |
| 既有两融、集中度浏览器脚本仍使用文案精简前的标题、标签和提示 | 按当前已验收界面更新定位；坏包仍验证 SHA 原因、无指标泄露及重试入口 | 图表浏览器验收；未为迎合旧断言改变产品文案 |

存储修复采用同步写前乐观检查，已阻止正常操作中长期打开的旧页覆盖新数据。它没有引入跨标签事务锁，不对极短窗口内真正同时发生的检查与写入作原子性保证。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| `npm test` | 31 个前端测试文件、222 个前端测试；52 个 Node 测试；74 个 Python 测试，合计 348 项通过 |
| 主 TypeScript、两融测试与验证器专用 TypeScript 配置 | 通过 |
| `npm run build` | 通过，保留原有大 chunk 提醒 |
| `npm audit --json --registry=https://registry.npmjs.org` | 0 个已知漏洞；这是依赖公告检查，不等同于不存在安全风险 |
| `npm run qa:research` | 通过：搜索歧义、保存/删除/未保存保护、详情失败重试、320–1440px 布局 |
| 双标签页及修饰键浏览器补充验收 | 通过；结果与截图保存在 `outputs/audit-debug-2026-09-05/` |
| `npm run qa:concentration` | 1440/768/390/320px 四视口及 HTTP、传输、SHA 校验失败三类场景通过；无非预期运行错误或溢出 |
| `npm run qa:leverage` | 通过：桌面/手机、实际缩放和手动日期、比例模式、SHA 坏包阻断、比例不可用包、离线边界及临时目录清理；正式数据前后 SHA 一致 |
| 组合发布包校验 | 2976 个股票分片、256 个详情分片通过；releaseId 为 `2026q2-20260905051834619843-4ee07ac05050` |
| 两融与集中度发布包校验 | 分别 3665 / 3316 条通过，截止 2026-09-03 |
| `git diff --check` | 通过 |

详细日志：`outputs/audit-debug-2026-09-05/tests.log`、`build.log`、`qa-concentration.log`、`qa-leverage.log`、`browser-regressions.json`。该目录为本地忽略的验收材料。已有设计截图在运行验收前保留原件，运行后恢复；新截图另存于本次验收目录。构建仅更新日期的 SEO 副产物已还原，避免无关发布变更。

原设计文件与构建副产物已逐一核对 SHA，确认恢复。自动审批审核拒绝删除本轮临时基线副本（`blocked by policy`），因此 `baseline-design-qa` 与 `baseline-build-generated` 保留在上述已忽略的验收目录。

## 保留事项与验证边界

- 当前申购限额快照为 2026-08-30，审计日已 6 天；预检正确显示时效警告。未将旧快照标记为已刷新。
- `public` 共有 37,912 个文件，7 个 Q2 版本目录中仅最新目录被当前 Q2 manifest 引用。开始任务时的 4 个未跟踪版本目录原样保留；未审计旧浏览器会话和历史发布的引用，因此没有直接删除历史数据。发布前需要结合实际套餐、发布方式和保留策略处理文件数量。
- 主入口约 1.11 MB（gzip 约 227 kB），ECharts 约 568 kB（gzip 约 192 kB），构建仍提示大 chunk。本轮未扩大为加载架构重构。
- 反馈接口用本地隔离环境验证请求边界，未进行真实 Turnstile 成功验证、SMTP 投递或生产高频请求；本次结果不证明这些外部集成已验证。
- 全部运行验证针对本地当前代码和静态数据，不能据此宣称线上已更新。按用户规则，待用户测试确认后再执行备份。

## 官方参考

异步清理与过期响应处理参考 [React useEffect](https://react.dev/reference/react/useEffect)；浏览器验收以 [Playwright 用户可见行为测试原则](https://playwright.dev/docs/best-practices) 为依据。

Browserslist 旧版风险与修复范围依据 [GHSA-c83g-rgw3-j3cx](https://github.com/advisories/GHSA-c83g-rgw3-j3cx) 和 [GHSA-73wf-gq98-2v4g](https://github.com/advisories/GHSA-73wf-gq98-2v4g)。该依赖位于 Babel 构建工具链，未发现网站将外部查询直接传给它的路径。

文件数量上限按 [Cloudflare Pages 当前限制](https://developers.cloudflare.com/pages/platform/limits/) 核对：免费套餐为 20,000，部分付费套餐支持 100,000；本轮未更改或核验账户套餐。
